#includestdio.h
#includestdlib.h
#includemath.h
int main()
{
	double i = 0;
	double Sn = 0;
	printf(ÇëÊäÈëÊı×Ön);
	scanf_s(%lf, &i);
	Sn = pow(i, 1) + pow(i, 2) + pow(i, 3) + pow(i, 4) + pow(i, 5);
	printf(%lf, Sn);
	printf(n);
	system(pause);
	return 0;
}